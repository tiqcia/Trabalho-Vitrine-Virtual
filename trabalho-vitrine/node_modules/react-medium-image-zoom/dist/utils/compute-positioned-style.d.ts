import type { ModalImgPosition } from './types.js';
export declare const computePositionedStyle: ({ containerHeight, containerLeft, containerTop, containerWidth, hasScalableSrc, offset, position, targetHeight, targetWidth, visibleHeight, visibleWidth, }: {
    containerHeight: number;
    containerLeft: number;
    containerTop: number;
    containerWidth: number;
    hasScalableSrc: boolean;
    offset: number;
    position: string;
    targetHeight: number;
    targetWidth: number;
    visibleHeight: number;
    visibleWidth: number;
}) => ModalImgPosition;
